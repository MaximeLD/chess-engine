package max.chess.engine.search.evaluator;

// Endgame activity (apply * gameProgress)
public final class KingEndgame {
  public static final int CENTER_STEP = +6; // reward for being closer to center (Chebyshev)
}